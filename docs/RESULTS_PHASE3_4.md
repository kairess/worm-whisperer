# Phase 3–5 결과: 프로토콜, 번역기, UI (2026-09-03)

코드: `worm/llm/protocols.py`(화이트리스트), `worm/llm/phrases.py`, `worm/llm/translator.py`, `worm/server/app.py`, `web/index.html`, `worm/env/chem.py`. 로그 `runs/phase3/`, `runs/phase4/`.

## 1. 회전(조향)과 정지 기전
- **조향**: Vfit 네트워크의 SMDD−SMDV 막전위 차이(ΔV)를 머리 6행의 선호 곡률 편향으로 잇는다 (Gray et al. 2005: SMD 가 머리 굽힘을 조향). 편향을 그대로 더하면 신장 수용기가 편향을 감지해 머리 단위가 한쪽에 고정되고 파동이 깨진다. Boyle 의 변형률 정의("국소 휴지 길이 기준")를 이용해 편향을 수용기의 기준 곡률로도 넘기면(수용기는 κ − κ_bias 만 감지) 파동을 유지한 채 몸이 휜다.
  - 실패한 대안: 정적 곡률 편향만 (등쪽 편향 시 정지), 머리 단위 문턱 편향 (파동 붕괴). `runs/phase3/steer_scan*.txt`
  - 채택 k_turn 0.06 (1/mm per mV): 왼쪽(SMDD) −26°/12 s, 오른쪽(SMDV) +27°/12 s, 기어가기 0.09–0.10 mm/s 유지.
- **정지(RIS)**: RIS ΔV 가 문턱(3 mV)을 넘고 명령/조향 뉴런 ΔV 의 1.5배보다 클 때 운동층을 끈다 (Turek et al. 2016 의 RIS 광활성화 → 즉시 정지). 이는 커넥톰 외 가정(펩타이드 FLP-11 효과)의 대체이며 플래그 `ris_gate` 로 분리. ALA 자극은 RIS 를 충분히 올리지 못해 정지시키지 못한다(정직한 부정 결과; Hill 2014 경로는 펩타이드 매개).

## 2. 프로토콜 화이트리스트 (docs/COMMANDS.md 의 코드화)
채널 19개(감각 8, 명령 4, 조향 5, 수면 2), 뉴런 37개. 운동뉴런·근육 없음. `validate()` 가 모든 스케줄을 검사한다. 프로토콜 13개의 행동 기술자 (`runs/phase3/protocols_final.txt`, 5 pA, 10–12 s, Vfit + Boyle 운동층):

| 프로토콜 | 등급 | 전진 속도 (mm/s) | 후진 비율 | 순 회전 | 파동 주파수 | 정지 비율 | 판정 |
|---|---|---|---|---|---|---|---|
| forward (AVB) | O | +0.176 | 0.00 | +9° | 0.56 Hz | 0 | ✓ |
| reverse (AVA) | O | −0.142 | 0.83 | +3° | 0.44 Hz | 0 | ✓ |
| stop | – | 0.000 | 0 | 0° | – | 1.00 | ✓ |
| turn_left (AVB+SMDV) | O | +0.164 | 0.00 | **+52°** | 0.67 Hz | 0 | ✓ (개정 전 +28°, 1.0 Hz) |
| turn_right (AVB+SMDD) | O | +0.123 | 0.13 | **−44°** | 0.56 Hz | 0 | ✓ (개정 전 −26°, 1.1 Hz) |
| omega_turn (AVA→SMDV/RIV→AVB) | O | +0.099 | 0.12 | **+174°** | – | 0 | ✓ 유턴 (개정 전 +3°; ADR-016) |
| local_search (춤) | O | +0.150 | 0.05 | **+369°** | – | 0 | ✓ 피루엣 2회/12 s (개정 전 +8°; 프로토콜 재정의) |
| head_sweep (SMD 교대) | O | +0.269 | 0 | −8° | 1.0 Hz | 0 | ✓ (개정 전 +0.404 mm/s, −21°) |
| quiescence_RIS (AVB + RIS) | O+X | +0.040 | 0 | +2° | – | **0.78** | ✓ 전진 중 정지 |
| quiescence_ALA (AVB + ALA) | O+X | +0.176 | 0 | +9° | 0.56 Hz | 0 | ✗ 정지 못 함 |
| escape (ASH 1 s) | S | 0.001 | 0 | −1° | – | 0.96 | ✗ 명령 선택 안 됨 |
| forward_touch (PLM 1 s) | S | −0.001 | 0.03 | +1° | – | 0.96 | ✗ |
| reverse_touch (ALM+AVM 1 s) | S | 0.000 | 0.03 | −1° | – | 0.94 | ✗ |

O 등급(광유전학 검증 뉴런 직접 자극) 9개 중 9개가 의도한 행동 (2026-09-03 개정: 유턴·춤·회전 강화, 아래 1b). S 등급(감각 자극)은 모두 실패(Phase 1 의 감각→명령 선택 실패와 같은 원인). 표는 `runs/phase3/protocols_final.txt` (개정 전 `protocols_final_before_omega.txt`). 좌/우 규약: 배쪽(SMDV) = 왼쪽(+), 등쪽(SMDD) = 오른쪽(−).

### 1b. 개정 (2026-09-03, Ubuntu 세션): 유턴 기전(ADR-016), 조향 범위, 춤 프로토콜
- **유턴이 안 되던 이유**: RIV/SMDV 는 +55/+51 mV 로 충분히 반응했으나 머리 편향(6 행)만으로는 몸의 진행 방향이 안 바뀐다. 앞쪽 절반의 정적 배쪽 편향은 κ −7 /mm 로 말리지만 파동이 멈추고 벌레가 빠져나오지 못한다(최대 +42°). `runs/phase3/omega_scan.txt`
- **채택**: RIV 활성(ΔV > 30 mV) 시 깊은 배쪽 굽힘 펄스(σ 4 행)가 머리→꼬리로 2.5 s 에 전파 (Croll 1975; Broekmans 2016 의 오메가턴 기술). 이득 0.36 에서 +162°(탐색), 표준 프로토콜 +174°. 진폭 의존: 2 pA 미발동(RIV 28 mV), 3 pA +53°, 4 pA +128°, 5 pA +172° (`omega_amp_scan.txt`). 이득 ≥ 0.45 는 과회전 후 파동 붕괴. 문턱 30 mV 는 SMD 자극 시 갭정션으로 새는 RIV 상승(≈ 20 mV)을 걸러 다른 프로토콜을 바꾸지 않는다 (`protocols_omega_check.txt`).
- **회전 시 1 Hz 주파수의 원인**: 머리 편향 6 행이 수용기 창(6 행 이동 창)과 어긋나 단위 1–2 가 잔여 편향을 감지. 편향 범위 12 행 + 이득 0.03 으로 주파수 0.55–0.67 Hz 회복, 회전은 오히려 ±50° 로 증가 (`turn_freq_scan.txt`, `turn_rows12_scan.txt`).
- **춤(local_search)**: 기존 정의는 RIV 를 쓰지 않았다(COMMANDS.md 와 불일치). 후진 1 s → 오메가 2 s → 전진하며 머리 좌우 3 s 의 6 s 주기로 재정의. 12 s 에 +369°(피루엣 2회), 활동량 0.157.
- 번역기 재학습(local_search 패턴 변경): 검증 정확도 0.834 ± 0.030 (변화 없음). 테스트 19개 통과.

## 3. 번역기 (옵션 B-1, behavior cloning)
- 임베딩: `paraphrase-multilingual-MiniLM-L12-v2`(384-d, 고정). 대안 `multilingual-e5-small` 비교.
- 구조: MLP → (프로토콜 혼합 p ∈ Δ¹³, 진폭 배율 a) → u = a·Σ p_c u_c. 출력이 항상 화이트리스트 패턴의 볼록 결합이라 구조적으로 원칙을 지키고, 혼합으로 합성 명령을 표현한다.
- 데이터: 프로토콜당 한국어/영어 문장 20개(총 260). 문장 단위 분할(프로토콜당 5개 검증), 5개 시드.

| 임베딩 | 검증 정확도 (5 시드) |
|---|---|
| MiniLM (채택) | 0.837 ± 0.025 |
| e5-small | 0.809 ± 0.037 |

- 새 표현 예: "살살 앞으로 가줘"→forward 0.72, "왼쪽으로 살짝 틀어"→turn_left 0.84, "졸려"→quiescence_RIS 0.75, "뒤로 천천히"→reverse 0.89, "오른쪽으로 가면서 춤춰"→local_search 0.88 + turn_right 0.06 (혼합).
- **행동 수준 평가** (`runs/phase4/behavior_eval.txt`): 프로토콜당 검증 문장 2개(26개)를 번역기 → 스케줄 → 시뮬레이션(12 s) → 행동 기술자로 평가.

| 지표 | 값 |
|---|---|
| 예측 프로토콜 = 의도 | 0.96 (25/26) → **1.00** (2026-09-03 개정: 유턴 문장 보강 + 디코딩 선명화) |
| 핵심 축(전진 부호, 회전 부호/크기) 일치 | 0.96 → **1.00** |
| 기술자 공간 최근접 프로토콜 = 의도 | 0.65 → **0.81** (유턴·춤·좌/우 회전이 행동 공간에서 분리됨) |

개정 후 남은 최근접 불일치는 번역기 오류가 아니라 프로토콜 자체가 행동적으로 겹치기 때문이다: quiescence_ALA 는 정지하지 못해 forward 와 같고, escape/터치는 모두 정지 상태이며, omega_turn 과 local_search 는 후진–전진 교대 패턴이 비슷하다. 유일한 번역 오류는 "뒤돌아" → reverse (의도 omega_turn).

## 4. 웹 UI (Phase 5)
`uv run uvicorn worm.server.app:app --port 8000` → http://localhost:8000. 채팅 입력 → 번역기 → 스케줄 → 50 ms 블록 시뮬레이션 → WebSocket 프레임(몸 좌표, 곡률, 근육 활성, 302뉴런 전압, 게이트, 상태 라벨과 근거). 실시간 배율 약 0.55 (Intel i7, 학습 병행 시 0.15).

## 5. S 등급 회전(화학주성)은 창발하지 않는다 (`runs/phase3/chemotaxis.txt`)
`worm/env/chem.py`: 가우시안 농도장, 머리 농도의 dC/dt 를 ASEL(증가)/ASER(감소) 전류로 (gain 50 pA per 농도/s, τ 0.5 s). AVB 전진 자극 병행, 20 s.

| 소스 위치 | 순 회전 | 소스 접근 거리 | 최대 ASE 전류 |
|---|---|---|---|
| 없음 | +9.1° | – | 0 |
| 왼쪽 (+y) | +9.5° | −1.49 mm (멀어짐) | 1.2 pA |
| 오른쪽 (−y) | +9.1° | −0.81 mm | 1.3 pA |
| 정면 (−x) | +9.0° | +3.06 mm (직진이라 접근) | 3.1 pA |

회전은 소스와 무관하다(모두 +9°, 무자극과 동일). 원인: (1) 0.18 mm/s 로 기어갈 때 농도 변화율이 작아 ASE 전류가 수 pA 이하, (2) Vfit 에서 ASE → SMD 경로의 이득이 조향에 미치지 못함(감각→명령 선택 실패와 같은 문제). 클리노택시스는 이 모델에서 재현되지 않으며, 회전 명령은 O 등급(SMD 직접 자극)으로만 성립한다.
