# worm-whisperer 프로젝트 계획

작성일: 2026-09-02
환경: macOS (Intel i7-8700, 16 GB, GPU 없음), 패키지 관리는 uv (Python 3.11 고정), Docker, Java 있음
갱신: 2026-09-02 신체 모델은 2D로 확정, Sibernetic 제외. LLM 연동은 API 방식(옵션 A)과 학습형 번역기(옵션 B) 병기, 7절 참조

---

## 1. 목표

**한 줄 정의**: 자연어 명령을 받아 *C. elegans* 302뉴런 커넥톰 시뮬레이션에 "실험 자극"을 주고,
그 결과로 나타나는 행동(전진, 후진, 회전, 탐색, 정지)을 실시간으로 보여주는 시뮬레이터.

### 1.1 반드시 지킬 원칙 (과학적 의미의 핵심)

1. **커넥톰 불변**: 뉴런 수(302), 시냅스/갭정션 연결, 근육 연결(95개 체벽근)은 OpenWorm c302 데이터 그대로 쓴다. 행동을 만들기 위해 연결을 임의로 추가/삭제하지 않는다.
2. **LLM은 조종사가 아니라 실험자**: LLM은 근육 활성값이나 운동뉴런(DA/DB/VA/VB/DD/VD)에 직접 값을 쓰지 못한다.
   LLM이 할 수 있는 일은 `docs/COMMANDS.md`에 등록된 **실험 프로토콜 화이트리스트** 중 하나를 고르고 파라미터(강도, 지속시간, 좌/우)를 정하는 것뿐이다.
3. **모든 프로토콜에 근거 논문**: 화이트리스트의 각 자극은 실제 광유전학/감각자극 실험에 대응한다. (예: AVA 광활성화 → 후진, RIS 광활성화 → 수면 상태)
4. **재현 가능**: 모든 실행은 시드, 자극 로그, 뉴런 전압, 근육 활성, 몸 궤적을 파일로 남긴다. 같은 자극 로그를 재생하면 같은 결과가 나와야 한다.
5. **검증 지표를 실제 실험값과 비교**: 전진 파동 주파수, 이동 속도, 후진 빈도, 오메가턴 각도, 정지 상태 지속시간을 문헌값과 비교한 표를 유지한다.

### 1.2 데모 시나리오 (최종 산출물)

| 명령 | 기대 행동 | 화면 표시 |
|------|-----------|-----------|
| "앞으로 가" | 전진 파동, 속도 ~0.1–0.2 mm/s | 상태: 전진 중 |
| "뒤로 가" | 후진 파동 후 자연스러운 재개 | 상태: 후진 중 |
| "왼쪽으로 가" | 머리 조향으로 궤적이 왼쪽으로 휨 | 상태: 왼쪽 회전 중 + 가상 유인 물질 위치 표시 |
| "춤춰봐" | 후진+오메가턴 반복, 머리 흔들기 | 상태: 춤추는 중 (local search) |
| "유튜브 보는중" | 이동 정지, 미세한 꿈틀거림 | 상태: 유튜브 보는중 (quiescence) |
| "AVA 없이 뒤로 가봐" | AVA 절제 상태에서 후진 실패 확인 | 상태: 절제 실험 |

마지막 항목이 이 프로젝트가 장난감이 아니라는 증거다. 같은 인터페이스로 절제(ablation) 실험을 돌릴 수 있다.

---

## 2. 시스템 아키텍처

```
┌────────────┐  자연어   ┌──────────────┐  프로토콜 JSON  ┌────────────────┐
│  웹 UI     │ ───────▶ │ LLM 컨트롤러 │ ─────────────▶ │  자극 스케줄러  │
│ (브라우저) │ ◀─────── │ (Claude API) │ ◀───────────── │  (화이트리스트) │
└────────────┘  상태 라벨└──────────────┘  요약 관측값    └───────┬────────┘
      ▲                                                          │ IClamp / 감각 입력
      │ WebSocket (뉴런 전압, 근육, 몸 좌표)                       ▼
┌─────┴──────────────────────────────────────────────────────────────────┐
│ 시뮬레이션 코어 (Python, 50 ms 제어 주기)                                │
│  ┌──────────────────┐   근육 활성 95ch   ┌────────────────────────────┐  │
│  │ 신경 모델 (c302)  │ ────────────────▶ │ 신체 모델                  │  │
│  │ 302 뉴런 + 95 근육│ ◀──────────────── │ 1단계: 2D 점탄성 막대      │  │
│  │ NEURON 백엔드     │  고유수용 피드백   │ 3단계: Sibernetic (3D SPH) │  │
│  └──────────────────┘                   └────────────────────────────┘  │
│  ┌──────────────────┐                                                   │
│  │ 가상 환경         │  감각뉴런 입력 (ASE/AWC/ASH/ALM/PLM)               │
│  │ 화학 농도장, 접촉 │                                                   │
│  └──────────────────┘                                                   │
└────────────────────────────────────────────────────────────────────────┘
```

### 2.1 신경 모델: c302

- OpenWorm `c302`는 커넥톰 데이터로부터 NeuroML2 네트워크를 생성한다. 파라미터 세트 A/B(적분발화형), C/C1/C2(단일구획 전도도 기반, 등급형 시냅스), D(다구획).
- **선택: C2 (기본), A (빠른 개발용)**. C2는 등급형(graded) 시냅스를 쓰는데, 실제 *C. elegans* 뉴런 대부분이 활동전위 없이 등급형 전위로 동작하므로 과학적으로 적절하다.
- 커넥톰 데이터 리더: c302 내장 `Cook2019` 리더 사용 (Cook et al. 2019 Nature 커넥톰). 옵션으로 Witvliet 2021 발달 커넥톰.
- **실행 방식이 핵심**: c302 기본 워크플로우는 자극을 NeuroML 파일에 미리 박아두고 한 번에 돌린다. 우리는 실시간 명령이 필요하므로,
  c302로 NeuroML/NEURON 코드를 생성한 뒤 **자체 스테핑 러너**로 대체한다.
  - NEURON을 Python에서 직접 로드 (`pip install neuron`, macOS x86 휠 있음)
  - 각 뉴런에 `IClamp` 객체를 미리 하나씩 붙여두고 `amp`만 실시간으로 바꾼다.
  - `h.continuerun(t + 50)` 루프로 50 ms씩 전진, 매 스텝 전압/근육 활성을 읽는다.
- **알려진 한계 (Phase 1에서 확인할 것)**: c302 기본 파라미터는 AVB 자극만으로 안정된 전진 파동을 만들지 못한다고 알려져 있다.
  c302의 `FW` 예제는 운동뉴런에 시차 자극을 직접 줘서 파동을 만든다. 파동이 자발적으로 나오게 하려면
  고유수용(proprioceptive) 피드백을 신체 모델에서 B형 운동뉴런으로 되돌려야 한다 (Wen et al. 2012, Fouad et al. 2018).
  이것은 커넥톰을 바꾸는 게 아니라 **감각 입력을 추가**하는 것이므로 원칙 1을 위반하지 않는다. 다만 문서에 명시한다.

### 2.2 신체 모델

**2D 점탄성 막대**로 확정 (Boyle, Berri, Cohen 2012 계열), 24 분절, numpy 또는 JAX.
벌레는 한천 위에서 옆으로 누워 기어가므로 등/배 굽힘 = 평면상 좌/우 회전이며, 2D로 좌/우 회전과 오메가턴까지 표현된다.
Sibernetic(3D SPH)은 사용하지 않는다 (ADR-001).

근육 매핑: c302의 95개 체벽근은 24행 × 4사분면(DL, DR, VL, VR, 앞쪽 일부 결손). 2D에서는 행별로 (DL+DR)/2 = 등쪽 토크, (VL+VR)/2 = 배쪽 토크.
환경 저항: 저항력 이론(resistive force theory), 한천 위 기어가기 기준 수직/접선 항력비 ≈ 40, 수영 기준 ≈ 1.5.

### 2.3 가상 환경

- 2D 평면에 화학물질 농도장(가우시안 소스)을 둘 수 있다. 머리 위치의 농도와 시간 미분이 ASEL/ASER(NaCl), AWC(휘발성 유인제)의 입력 전류가 된다.
- 접촉 자극: 앞쪽(ALM/AVM), 뒤쪽(PLM) 터치를 시간 펄스로 준다.
- "왼쪽으로 가"의 2단계 구현: 머리 조향 뉴런(SMD/RMD) 직접 자극이 아니라 **왼쪽에 유인 물질 배치 → 화학주성(klinotaxis)이 창발**하는지 본다. 창발되면 그것이 논문감이다.

### 2.4 LLM 컨트롤러

- Claude API의 tool use로 구현. 도구는 딱 세 개:
  - `run_protocol(name, params)` : 화이트리스트 프로토콜 실행
  - `set_environment(source_xy, chemical, concentration)` : 가상 환경 변경
  - `ablate(neurons)` / `restore()` : 절제 실험 (별도 확인 플래그 필요)
- 시스템 프롬프트에 `docs/COMMANDS.md`의 표를 그대로 넣는다. LLM이 표 밖의 뉴런을 요구하면 스케줄러가 거부한다.
- LLM은 매 명령마다 **상태 라벨** ("유튜브 보는중") 과 **선택 근거** (어떤 논문 프로토콜을 왜 골랐는지)를 함께 반환한다. 이 근거가 UI에 표시된다.
- 폐루프 옵션 (Phase 6): LLM이 50 ms마다가 아니라 2–5초마다 요약 관측값(속도, 방향, 후진 여부)을 받아 자극을 재조정한다. 이것이 "LLM 실험자"의 진짜 형태다.

### 2.5 UI

- 백엔드: FastAPI + WebSocket. 프론트: 단일 HTML/Canvas (프레임워크 최소화).
- 화면 구성: (1) 벌레 평면도 + 환경 농도장, (2) 302뉴런 전압 래스터 (그룹별 정렬: 감각/개재/운동), (3) 근육 95ch 히트맵, (4) 채팅창 + 상태 라벨 + 선택된 프로토콜과 근거.

---

## 3. 저장소 구조

```
worm-whisperer/
├── README.md, PLAN.md
├── docs/
│   ├── COMMANDS.md        # 명령 ↔ 프로토콜 ↔ 뉴런 ↔ 논문
│   ├── REFERENCES.md
│   ├── DECISIONS.md       # ADR
│   └── VALIDATION.md      # 문헌값 vs 시뮬레이션값 표 (Phase 2부터 채움)
├── worm/
│   ├── neural/            # c302 생성, NEURON 스테핑 러너, 자극 API
│   ├── body/              # rod2d.py, muscle_map.py
│   ├── env/               # 화학장, 접촉
│   ├── llm/               # protocols.yaml, controller_api.py (옵션 A), translator/ (옵션 B)
│   └── server/            # FastAPI, WebSocket, 로깅
├── web/                   # index.html, worm.js
├── experiments/           # 검증 노트북, 절제 실험 스크립트
├── tests/
└── runs/                  # 실행 로그 (git 제외)
```

---

## 4. 단계별 로드맵

### Phase 0. 환경 구축 — 완료 (2026-09-02)
- [x] uv 프로젝트 (Python 3.12), c302 0.12.0, pyNeuroML 1.3.22, NEURON 9.0.2, JAX 0.4.38 (Intel Mac 마지막 지원 버전)
- [x] OpenJDK 21 (Homebrew), nrnivmodl 컴파일 우회 (README 참조)
- [x] `experiments/phase0_c302_reference.py`: 생성 → NEURON 내보내기 → 컴파일 → 실행 → 그림, 한 번에
- [x] 전체 302뉴런 C2 실행 성공. 결과는 `docs/VALIDATION.md`
- **발견**: c302 FW 예제의 전진 파동은 근육에 시차 전류를 직접 넣어 만든 것. 창발이 아님. 부분회로는 자극 시 폭주하지만 **전체 네트워크에서는 AVB→B형, AVA→A형 운동뉴런 분화가 커넥톰만으로 나온다.** 파동(진동)은 아직 없음.

### Phase 1. 신경 엔진: c302 C2의 JAX 재구현 (약 2주)
사양은 `docs/VALIDATION.md` 하단. 파라미터 A/B는 쓰지 않는다 (C2가 이미 실행 가능하고 등급형 시냅스가 생물학에 맞음).
- [x] `worm/neural/connectome.py`: c302가 생성한 `.net.nml`을 파싱해 (뉴런 목록, 화학 시냅스 [pre, post, g, 극성, w], 갭정션 [a, b, g], 근육 연결) 배열로 추출. NeuroML이 유일한 진실 원천이므로 리더 교체(Cook2019 등)가 자동 반영됨
- [x] `worm/neural/jaxsim.py`: 뉴런 397개(302 + 근육 95) 상태 벡터 [V, n, p, q, e, f, Ca] + 시냅스 s. `lax.scan`, dt 0.05 ms, 지수 오일러
- [x] `WormSim.run(block_ms, {neuron: pA})`: 외부 전류 벡터 I_ext (397,) 를 50 ms 블록마다 갱신하는 인터페이스
- [x] 검증: NEURON과 RMS 0.001 mV (뉴런). `tests/test_jax_vs_neuron.py` 4개 통과. 결과 `docs/RESULTS_PHASE1.md`
- [x] 속도: dt 0.25에서 0.71 s (float32) / 1.05 s (float64), dt 0.05에서 3.5 / 5.3 s
- [x] 터치 회로 실험 실행. 결과: 기본 파라미터는 지속 자극에 전역 상향 상태로 전이해 해석 불가 (RESULTS_PHASE1 2–3절)
- [x] Cook 2019 리더 어댑터(`worm/neural/readers/`), 두 커넥톰 모두 NEURON과 일치 확인
- [x] **Phase 1b**: 변형 V0–V3를 Randi 2023 신호전파 아틀라스와 비교. 결론: 동역학 모델 > 해부학(유의), 예측력의 원천은 갭정션, 화학 시냅스는 꺼져 있음, 상관 지표는 측정 상한에 근접 (`docs/RESULTS_PHASE1.md` 5절)
- [x] **Phase 1c**: 전역 파라미터 6개 경사 적합 → 변형 `Vfit`. 검증 세트 AUROC 0.646, Spearman 0.131 (상한 0.14), 폭주 없음, 터치 회로 논리 정성 재현 (ADR-011)
- **완료 기준(갱신) 판정**: AUROC ↑ (경계 유의), 폭주 없음 ✓, 억제 재현율 0 ✗ → Phase 1 은 조건부 완료. 억제 재현은 미해결 과제로 Phase 2b 와 함께 다룬다.
- **완료 기준**: JAX 시뮬레이터가 NEURON과 일치하고, 코드로 AVA/AVB 전류를 바꾸면 운동뉴런 클래스가 전환된다.

### Phase 2. 신체 모델 + 폐루프 (진행 중, 결과 `docs/RESULTS_PHASE2.md`)
- [x] `worm/body/rod2d.py`: 24분절 점탄성 막대, 저항력 이론, 선호 곡률 입력. 진행파 직접 입력 시 0.24 mm/s (문헌 범위). 테스트 4개
- [x] `worm/sim.py`: 신경–신체 결합(근육 Ca → 활성 → 곡률), 고유수용 옵션(이득, 오프셋, 부호)
- [x] 고유수용 피드백 실험(Wen 2012 방식, Boyle 2012 방식): **이동 창발 없음**. 고립 세포에 고유 진동 없음 (계단형 세포)
- [x] **Phase 2b**: 세포 모델 확장(Ca 활성 K, 느린 K) 탐색 → 느린 이완 진동 불가 확인 (RESULTS_PHASE2 9–10절)
- [x] **Phase 2c**: Boyle 2012 운동층 채택 (ADR-012). 전진 0.18 mm/s, 0.5 Hz 창발. Vfit 의 AVB/AVA 로 게이트 → 명령 자극으로 전진/후진 (12절)
- [ ] 감각→명령 선택(PLM→전진, ALM→후진) 정합: 억제를 포함하는 적합 목표 (Phase 1c 후속)
- [ ] 두 가설을 같은 운동학 지표로 비교. 후보가 문헌값 ±50% 안에 들어오면 채택
- **완료 기준(갱신)**: 하나의 문서화된 최소 가정 세트로 AVB 자극 시 전진, AVA 자극 시 후진이 나오고, 주파수·파장·속도가 문헌값 ±50% 안에 있다. 창발하지 않으면 그 사실과 탐색 범위를 보고한다.

### Phase 3. 명령 프로토콜 화이트리스트 — 완료 (2026-09-03, `docs/RESULTS_PHASE3_4.md`)
- [x] `worm/llm/protocols.py`: 13개 프로토콜, 채널 19개, `validate()`
- [x] 회전: O 등급(SMD 자극 → 머리 곡률 편향, ADR-013) 동작. S 등급(화학주성, `worm/env/chem.py`)은 창발하지 않음 (부정 결과 기록)
- [x] 정지: RIS 게이트(플래그 `ris_gate`, 커넥톰 외 가정 명시). ALA 는 정지 못 시킴
- [x] 동작하는 명령: 전진, 후진, 정지, 좌/우 회전(±50°/12 s), 유턴(+174°, ADR-016), 국소 탐색(춤, 피루엣), 머리 흔들기, RIS 정지. 동작하지 않는 것: 감각 프로토콜(ASH 도망, 터치)

### Phase 4. 번역기 (옵션 B-1) — 완료 (2026-09-03)
- [x] 로컬 다국어 임베딩(MiniLM) → 프로토콜 혼합 헤드 → 자극 패턴 (ADR-014). 문장 267개, 검증 정확도 0.86 ± 0.02 (디코딩 선명화 γ=2)
- [x] 상태 라벨과 근거(프로토콜 표의 논문) 반환, 화이트리스트 구조적 강제
- [x] 행동 수준 평가 (`runs/phase4/behavior_eval.txt`)
- [ ] B-2 시뮬레이터 루프 미세조정 (운동층 불연속 → 무경사 최적화 필요, 미착수)

### Phase 5. UI — 완료 (2026-09-03)
- [x] `worm/server/app.py` + `web/index.html`: 채팅 → 번역기 → 시뮬레이션 → 몸/래스터/근육/상태 라벨 스트리밍. 실시간 배율 약 0.55
- [ ] 실행 로그 저장/재생

### Phase 6. 과학적 확장 (진행형)
- 절제 실험 배치: 각 명령에 대해 관련 뉴런 절제 시 행동 손실 여부를 자동으로 표로 생성 → 문헌 절제 실험과 비교
- 폐루프 LLM 실험자: 목표("유인 물질까지 도달")를 주면 LLM이 자극을 반복 조정
- 커넥톰 버전 비교: Cook 2019 vs Witvliet 2021 (발달 단계별) 에서 같은 명령의 행동 차이
- 결과 정리: 워크숍/프리프린트 수준의 짧은 보고서. (이 세션에는 학술 논문 파이프라인 스킬(ARS)이 있으므로 필요 시 활용)

---

## 5. 리스크와 대응

| 리스크 | 가능성 | 대응 |
|--------|--------|------|
| c302 기본 파라미터로 창발 행동이 안 나옴 | 높음 | 고유수용 피드백 추가, 파라미터 세트 A/B/C2 비교, 조정 범위를 문서화. "안 나온다"는 결과 자체도 기록 가치 있음 |
| C2가 실시간에 못 미침 | 중간 | 개발은 A/B, 검증은 C2. 장기적으로 numpy/JAX 재구현 (같은 커넥톰 CSV 사용) |
| 정지 상태(수면)는 펩타이드 없이 재현 불가 | 높음 | 펩타이드 층을 명시적 확장으로 추가, 플래그로 분리 |
| LLM이 표 밖 뉴런을 요구 | 중간 | 스케줄러 단에서 하드 거부. LLM은 신뢰 경계 밖 |
| 학습형 번역기가 회로를 우회해 운동뉴런을 직접 구동 | 높음 | 출력 채널을 감각/명령 뉴런으로 제한, 희소성 페널티, 우회 여부를 정량 측정 (7절) |

---

## 6. 첫 주에 할 일 (구체적 순서)

1. `uv init --python 3.11 && uv add c302 pyneuroml neuron numpy fastapi uvicorn`
2. `python -c "import c302; print(c302.__version__)"` 및 `nrniv --version` 확인
3. c302 저장소 클론, `examples/` 에서 `c302_C2_FW` 실행, 출력 파일 구조 파악
4. `worm/neural/runner.py` 뼈대: 생성된 NeuroML을 NEURON으로 로드하고 10 ms만 돌려 전압 출력
5. c302 A/B 모델을 numpy로 재구현할 수 있는지 c302 소스(셀 모델, 시냅스 식)를 읽고 판단 (옵션 B의 전제)
6. `docs/DECISIONS.md` ADR-005 (옵션 A/B) 결정

---

## 7. LLM 연동 방식: 옵션 A (API) vs 옵션 B (학습형 번역기)

### 옵션 A. Claude API + 프로토콜 화이트리스트
2절과 4절에 쓴 방식. 빠르게 데모가 나오고, 매 명령의 근거 논문을 LLM이 설명한다.
단점: 명령 → 행동이 사람이 손으로 짠 표에 갇힌다. 새로운 것을 배우는 부분이 없다.

### 옵션 B. LLM 특징 벡터 → 뉴런 자극 번역기 학습 (제안)

**정의**: 로컬 소형 LM(예: 0.5–1.5B 디코더, 또는 문장 임베딩 모델)을 고정(frozen)하고,
명령 문장의 중간층 은닉 상태 `z ∈ R^d`를 입력받아 **시간에 따른 자극 패턴** `u(t) ∈ R^{K×T}`를 출력하는 작은 번역기 `f_θ`를 학습한다.
`u`는 커넥톰 시뮬레이터에 주입되고, 행동은 시뮬레이터가 만든다.

**핵심 제약 (이게 없으면 번역기는 그냥 꼭두각시 조종기가 된다)**:
- 출력 채널 K는 302개 전체가 아니라 **생물학적으로 외부 입력이 들어오는 지점**만: 감각뉴런 쌍(ASE, AWC, ASH, ALM/AVM, PLM 등 ~12쌍), 광유전학으로 검증된 명령 개재뉴런(AVA, AVB, RIS, ALA, SMD 등 ~8개), 펩타이드/모노아민 층 스칼라(세로토닌, PDF, FLP-11 ~3개). 합쳐서 K ≈ 25–30.
- 운동뉴런(DA/DB/VA/VB/DD/VD)과 근육은 출력 공간에서 제외. 절대 예외 없음.
- 희소성 페널티(L1)와 진폭 상한. 번역기가 "많은 뉴런을 조금씩" 쓰는 대신 "소수 뉴런을 확실히" 쓰게 유도한다. 학습 후 어떤 뉴런을 쓰는지 보는 게 결과다.

**학습 신호**: 명령 문장에 대한 목표를 **행동 기술자(behavior descriptor)** `b ∈ R^m`로 정의한다.
예: 전진 속도, 진행 방향 변화율, 후진 비율, 오메가턴 빈도, 활동량(근육 활성 총합), 머리 흔들기 진폭. m ≈ 8.
- 손으로 만드는 것은 문장이 아니라 **행동 기술자와 문장 템플릿 몇십 개**뿐이다. ("왼쪽으로 가" → 방향 변화율 +, 속도 +) 동의어/변형 문장에 대한 일반화는 LM 임베딩이 담당한다.
- 손실: `L = || b(sim(f_θ(z))) − b_target ||² + λ₁||u||₁ + λ₂ (진폭 상한 위반)`

**시뮬레이터를 학습 루프에 넣는 방법** (둘 중 하나, 1-b 권장):
1. NEURON 기반 c302를 그대로 쓰고 무경사 최적화(CMA-ES) 또는 PPO. 시뮬레이션이 느려서(실시간 근처) 데이터가 부족. 단순 명령 6개 정도는 가능하나 일반화 실험엔 모자람.
2. **c302 A/B 수준(적분발화 + 등급형 시냅스)을 JAX로 재구현**해 미분 가능하게 만든다. 2D 막대도 JAX로 쓰면 문장 → 자극 → 뉴런 → 근육 → 궤적 전체가 미분 가능해져 경사 하강으로 번역기를 학습할 수 있다. 배치 시뮬레이션이 CPU에서도 빠르다.
   - 충실도 검증: 같은 자극에 대해 JAX 재구현과 c302/NEURON 원본의 전압 궤적을 비교해 오차를 문서화. 커넥톰 데이터(뉴런, 시냅스 가중치, 근육 연결)는 c302가 읽는 것과 동일한 파일을 쓴다.
   - 이 "미분 가능한 벌레" 자체가 독립적인 기여다.

**단계**:
- B-1. 부트스트랩: `docs/COMMANDS.md`의 손으로 짠 프로토콜을 교사 신호로 삼아 번역기를 지도학습 (behavior cloning). 이 단계에서 이미 옵션 A와 같은 데모가 나온다.
- B-2. 시뮬레이터 루프 학습: 행동 기술자 손실로 미세조정. 프로토콜 표에 없는 조합("천천히 왼쪽으로", "뒤로 가면서 춤춰")이 되는지 본다.
- B-3. 분석: (a) 번역기가 각 의미 축(전진/후진, 좌/우, 활동량)에 어떤 뉴런을 쓰는지 → 생물학과 비교. AVA를 후진에, ASE/SMD를 회전에 스스로 찾아내는가? (b) 절제 실험: AVA 절제 시 학습된 번역기의 후진도 실패하는가? (c) 제약을 풀었을 때(운동뉴런 허용) 번역기가 회로를 우회하는 정도를 정량화 → 제약의 필요성을 보여주는 대조 실험.

**과학적 의미**: 언어 표상을 고정된 생물학적 회로에 접지(grounding)시키는 실험이다. 인간 fMRI/ECoG와 LM 임베딩을 맞추는 연구(Schrimpf 2021, Goldstein 2022)의 역방향이며, 회로 구조가 완전히 알려진 유일한 동물에서 한다는 점이 차별점이다. 결과가 "번역기는 생물학이 쓰는 뉴런을 재발견한다"이든 "재발견하지 못하고 대체 경로를 쓴다"이든 둘 다 보고 가치가 있다.

**리스크**:
- c302 A/B의 JAX 재구현 공수 (약 2주 추가). 시냅스 모델(등급형, 갭정션)과 근육 셀 모델을 정확히 옮겨야 한다.
- 커넥톰에서 창발 행동이 안 나오면 번역기가 아무리 잘해도 안 된다. Phase 1–2의 검증이 먼저다. 번역기는 "창발이 나온다"가 확인된 뒤 시작한다.
- 행동 기술자 설계가 결과를 좌우한다. 기술자는 문헌의 행동 정량화 지표(속도, 후진 빈도, 오메가턴, 정지 비율)를 그대로 쓴다.

**권장**: 옵션 B로 간다. 단, B-1(behavior cloning)까지는 옵션 A의 프로토콜 표가 그대로 교사 데이터이므로 표 작업은 버려지지 않는다. Phase 4를 "옵션 B-1"로 바꾸고, Phase 6에 B-2/B-3을 둔다. Phase 1의 신경 엔진은 처음부터 JAX 재구현을 목표로 하되, c302/NEURON 원본은 검증 기준으로 유지한다.
